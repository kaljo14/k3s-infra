apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: alertmanager
  managed-by: kustomize

resources:
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

