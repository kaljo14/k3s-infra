apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
data:
  config.js: |
    window.__RUNTIME_CONFIG__ = {
      API_BASE_URL: "http://emailer-service.emailer.svc.cluster.local:80"
    }

